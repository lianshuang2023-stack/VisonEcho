# VisionEcho 本地运行

VisionEcho 是一个视频无障碍工作室。视频、版本和编辑文件保存在本机；对白识别、画面理解和解说配音调用已配置的 Azure 服务。

## 启动

```bash
cd VisonEcho
./run-local.sh
```

网页：<http://127.0.0.1:5174/>。API 文档：<http://127.0.0.1:8000/api/docs>。按 Ctrl+C 停止两个服务。

## 视频制作

1. 首页点击“上传视频”，选择已有项目、新建项目或暂不分类，加入 MP4。默认限制 500 MB、10 分钟。
2. 在“我的作品”打开视频。原片对白语言可选自动识别、中文或英文；解说语言与音色独立选择。
3. 选择口述方式并点击生成。自动模式优先使用自然对白间隙；若没有足够间隙，会在句间暂停画面加入解说，原对白完整保留且成片会变长。自然间隙模式保持原时长，扩展模式始终为解说增加暂停时间。
4. 查看口述稿、对白字幕与视频；编辑口述稿或切换“此版本配音音色”后，可重新配音并生成新版本。
5. 从“导出文件”下载 MP4、SRT/VTT 字幕或 TXT 口述稿。

对白语言决定原声识别，解说语言决定新口述稿与配音。页面中英文切换和亮暗主题只影响界面。字幕保留原声语言，现有版本换音色时保持其口述稿语言。

| 语言 | 可选音色 |
|---|---|
| 中文普通话 | 晓晓（女）、云希（男）、晓伊（女） |
| 英语 | Jenny（女）、Guy（男）、Aria（女） |

## 字幕校准

新任务采用 Azure Speech SDK 连续识别，读取最终识别结果的词级时间戳；自动模式识别普通话和英语。长句按词边界与句末拆分，保留标点、英文空格和数字格式。无对白的空识别结果正常继续画面解说；有文字但缺少词时间戳时停止，避免把对白当作静音。

未人工修改的旧机器字幕会在读取时修正排版，原始文件保留。识别内容需要更新时，打开“对白字幕”，选择原片对白语言，保存当前编辑，再点击“重新校准字幕”。确认后从原片重新识别并替换此版本字幕，原始音频和现有解说配音保持不变。校准期间若其他窗口保存了字幕，系统保留新修改并提示重试。

识别仍受录音噪声、发音和语言选择影响。字幕支持手动校对；程序不会虚构缺失词时间或均匀分配整段音频。

## 配置与依赖

根目录 .env.local 为后端私有配置（权限600，Git忽略）。前端没有密钥。模板见 .env.azure.example。依赖为 Python3.12、Node22/24 LTS、FFmpeg/FFprobe、Azure官方Speech SDK。

```bash
uv venv --python 3.12 --seed .venv
uv pip install --python .venv/bin/python -r local_backend/requirements-dev.txt
cd dashboard/frontend
npm ci
```

配置修改后重启服务。Speech可以使用资源自定义端点，或明确的区域配置；如果Speech与OpenAI属于不同资源，可单独设置 AZURE_SPEECH_KEY。密钥不要写入前端变量。

## 验证

```bash
.venv/bin/python -m pytest local_backend -q
cd dashboard/frontend
VITE_LOCAL_BACKEND=true npm run build
./node_modules/.bin/vitest run
```

测试使用合成媒体与模拟服务响应，覆盖语言选择、字幕时间、镜头抽帧、版本继承、扩展导出、删除恢复及错误处理。测试通过不能保证所有实际视频的识别准确度。`python -m local_backend.check_quality_smoke` 可显式执行少量 Azure 测试调用，使用脚本生成的中英文测试音频和几何图像，会产生用量。

所有任务共用单任务锁；生成、重配音、字幕校准会调用Azure并按量计费。字幕文本手动保存、预览、文件导出在本地完成。

## 官方接口依据

- [Speech SDK 词级时间与最终识别结果](https://learn.microsoft.com/en-us/azure/ai-services/speech-service/get-speech-recognition-results?pivots=programming-language-python)
- [Speech 自定义端点](https://learn.microsoft.com/en-us/azure/ai-services/speech-service/speech-services-private-link#construct-endpoint-url)

原仓库 AWS 部署代码保留；显式本地模式使用 VisionEcho 工作区。

## 项目与回收站

首页统一为“我的作品”，默认显示全部未归档视频，按最近编辑排序。项目用于分类与筛选，可通过“项目管理”新建、重命名和删除。旧默认项目在界面显示为“未分类”。

作品右上角“…”菜单提供重命名、移动到项目、归档和删除；项目管理中也可删除分类。删除后进入回收站，原始视频、字幕与历史版本保留，可恢复；删除整个项目会同时隐藏其下所有视频。正在上传、生成或校准时，相关项目/视频暂不可删除。归档仍是项目内视频的独立分类，不等同删除。

项目与作品删除采用可恢复方式，不删除媒体和历史版本。

## 制作工作台

顶部只保留品牌和工作区菜单；设置和回收站收进工作区菜单。项目、制作状态、归档与排序集中到同一工具栏，搜索支持视频或项目名称。上传、编辑后自动更新，生成中的作品自动轮询。

制作状态包括待生成、生成中、待校对、可导出和生成失败。生成成功后显示待校对，可预览、编辑和导出；已有人工审核记录会显示可导出。归档状态独立于制作状态。

## 对白密集视频修复

部分视频一直有对白，因此旧版本没有调用画面模型和TTS，却被显示为成功。现在通过outcome明确区分仅字幕与已生成口述；无口述不能标记为可导出。自动模式找不到自然空隙时使用扩展口述，最多4个句间暂停，每段最多8秒解说；字幕、对比播放与重新配音会按新的成片时间轴处理。

## 当前已知限制

- 普通模式对带非零音轨起点的视频存在混音错位问题；扩展模式单独处理音频时间轴。
- 校准完成后的索引写入失败，可能出现任务报告失败但字幕已经替换；仍需完善提交回滚。
- 生成完成到新版本加载之间的网络延迟或失败，可能导致草稿丢失或结果未自动刷新。
- 校准中的页面刷新或新标签页不会自动恢复任务跟踪；可在任务结束后重新打开作品。
- 字幕编辑器目前支持修改文字与时间，不提供新增、删除行。
