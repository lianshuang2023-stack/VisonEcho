# VisionEcho 本地运行

[English](RUN-LOCAL.md) | **简体中文**

VisionEcho 是口述电影制作工作台。视频、历史版本和编辑文件保存在本机；对白识别、画面理解、人物分析和解说配音使用已配置的 Azure 服务。本地模式不需要账号，托管试用的次数限制不适用于本地工作区。

## 配置与依赖

需要 Python 3.12、Node.js 22 或 24 LTS、FFmpeg / FFprobe，以及可用的 Azure OpenAI 和 Azure Speech 资源。以下命令均从仓库根目录执行：

```bash
python3.12 -m venv .venv
.venv/bin/python -m pip install -r local_backend/requirements-dev.txt
npm --prefix dashboard/frontend ci
```

首次运行时，根据 [.env.azure.example](.env.azure.example) 创建 `.env.local`；已有配置时保留原文件：

```bash
cp .env.azure.example .env.local
chmod 600 .env.local
```

在本机编辑器内填写服务配置。`AZURE_OPENAI_DEPLOYMENT` 必须与资源内实际部署名一致；模板默认值为 `gpt-5.6-terra`。Speech 可以使用资源自定义端点或明确的区域配置；Speech 与 OpenAI 属于不同资源时，单独填写 `AZURE_SPEECH_KEY`。配置修改后重启服务。

`.env.local` 仅供后端读取，已由 Git 忽略。不要将密钥写入前端变量、文档或提交记录。

## 启动

```bash
./run-local.sh
```

- 网页：[http://127.0.0.1:5174/](http://127.0.0.1:5174/)
- API 文档：[http://127.0.0.1:8000/api/docs](http://127.0.0.1:8000/api/docs)

打开网站根地址会先显示英文介绍页，可切换为中文并进入工作台。工作台可以切换页面语言及亮暗主题，这些设置不会翻译字幕或改变解说语言。按 `Ctrl+C` 停止两个服务。

## 视频制作

1. 在“我的作品”点击“上传视频”，选择已有项目、新建项目或暂不分类，加入 MP4。本地默认单文件上限为 500 MiB、时长上限为 10 分钟，可通过后端环境变量调整。
2. 打开视频，选择原片对白语言：自动识别、中文、英文或无对白。解说语言与音色独立选择，支持英文原声生成中文解说，反之亦然。
3. 选择口述方式并生成。自动模式优先使用自然对白间隙；间隙不足时暂停画面插入解说，保留原对白，成片会变长。自然间隙模式保持原时长；扩展模式为解说增加暂停时间。
4. 预览视频、口述稿和对白字幕。可以直接下载已生成文件，也可以按需审校；审校标记、质量提示和人物确认不是导出门禁。修改口述稿或切换当前版本音色后，点击“重新配音并保存新版本”应用到成片。
5. 从导出菜单下载 MP4、SRT / VTT 字幕或 TXT 口述稿。下载对应已保存的版本，未保存草稿不会自动进入成片。

对白语言决定原声识别，解说语言决定新口述稿与配音。字幕保留原声语言；已有版本仅更换音色时，口述稿语言保持不变。

| 解说语言 | 可选音色 |
|---|---|
| 中文普通话 | 晓晓（女）、云希（男）、晓伊（女） |
| 英语 | Jenny（女）、Guy（男）、Aria（女） |

## 字幕、多说话人与无对白素材

Azure Speech SDK 读取最终识别结果及词级时间戳，支持普通话和英语自动识别。当前 SDK 可用时采用对话转写区分说话人，否则使用连续识别。说话人标签来自音频，不代表真实姓名，也不会自动关联画面人物。多人抢话可能漏识别，需核对原声。

长句按测得的词边界和句末拆分。原始识别记录保留标点；字幕预览和导出移除句子标点，保留单词内部的撇号、数字的小数点与千位分隔符，以及英文单词间的空格。字幕编辑器支持修改文字、起止时间、说话人，以及新增和删除字幕。不同且已明确标注的说话人可以有重叠字幕；同一人或未知说话人的重叠时间会提示修正。低置信度对白会显示核对提示。

确认素材只有音乐或环境音时，可选“无对白”，跳过语音识别后生成画面口述。有声素材识别不到文字，不会直接视为静音：系统标记“对白未可靠识别”；自动或扩展模式将解说安排在原片末尾，避免覆盖未知对白。无音轨或接近静音的素材会显示“没有可转写对白”。有文字但缺少有效词时间戳时会停止，避免使用虚构时间。

未人工修改的旧机器字幕会在读取时修正排版，原始文件保留。若识别文字需要更新，选择原片对白语言，保存当前编辑，再点击“重新校准字幕”。确认后从原片重新识别并替换当前版本字幕；原声与解说配音保持不变。识别失败或校准期间其他窗口保存了字幕时，保留现有修改。选择“无对白”再校准会清空当前版本字幕，不调用语音识别。

## 画面依据与人物卡

口述稿下方可展开“查看画面依据”，查看关键帧、原片时间及生成时记录的画面观察。点击画面可跳至原片对应位置。旧版本缺少生成依据时，可在本地补抽校对画面并明确标注；这些补抽画面不代表模型生成时曾使用的证据。画面观察是核对辅助，不是准确性保证。

可勾选“人物有误”“动作有误”“内容遗漏”并保存备注。保存纠错意见本身不调用云服务，也不会自动改写已生成音频。口述稿可手动编辑；使用 AI 改写时会单独调用云服务，修改后的文字需重新配音才能进入新成片。

生成设置默认开启“自动识别人物”，从关键帧生成外观描述、缩略图及出现段落。已完成视频也可在“项目资源”的人物卡内单独识别，不必重新制作配音。每次最多检查 12 个口述区间的 24 帧，属于抽样分析，可能遗漏人物。

具有明确标志性服装、面具、徽记或动画设计的虚构角色，可以在视觉依据充分时自动填写角色名称。不会从面孔推断真实演员或普通人的身份；不确定的角色以外观区分。已有人工称呼不会被自动覆盖；修改自动名称后采用人工设置。人物卡最多 40 张，识别失败可单独重试。

也可从关键帧“添加人物”，填写外观、统一称呼和别名，或将画面关联到已有卡片。不确定是否为同一人时保留不同卡片。人工确认或自动识别的称呼仅在画面明确匹配时作为生成参考。改名或“检查称呼”会提供相关段落及替换预览，应用后只修改草稿；历史版本文字和音频不会自动改写。

## 项目、历史版本与回收站

“我的作品”默认显示未归档视频，按最近编辑排序。项目用于分类与筛选，“项目管理”提供新建、重命名和删除；默认项目显示为“未分类”。搜索支持视频名或项目名，上传和编辑后自动更新，处理中作品自动刷新进度。

作品“…”菜单提供重命名、移动到项目、归档和删除。删除采用回收站方式，保留原视频、字幕和历史版本，可恢复；删除项目会同时隐藏该项目的视频。归档独立于制作状态，也不等同于删除。正在上传或处理的相关作品暂不可删除。

“待校对”和“可导出”用于说明制作及审校进度。已有生成文件即可导出；仅字幕、部分口述或无口述结果会明确提示，不能当作完整口述制作已完成。自动扩展模式目前最多安排 4 个暂停位置，每段口述预算最多 8 秒；字幕和播放时间轴随成片调整。

## 验证

```bash
.venv/bin/python -m pytest local_backend -q
npm --prefix dashboard/frontend run build
cd dashboard/frontend
./node_modules/.bin/vitest run
```

测试使用合成媒体和模拟服务响应，覆盖语言选择、多说话人字幕、无对白处理、时间轴、画面抽帧、版本继承、扩展导出、删除恢复和会话隔离。测试通过不代表所有视频都有相同识别准确度。

若需要显式测试已配置的 Azure 服务，从仓库根目录运行以下命令。它使用脚本生成的音频及几何图像，会产生实际用量：

```bash
.venv/bin/python -m local_backend.check_quality_smoke
```

所有媒体任务共用一个处理槽。生成、重新配音、字幕校准、人物识别和 AI 改写可能调用 Azure；文本手动保存、预览和下载已有文件在本地完成。

## 运行限制

- 画面描述依赖抽样关键帧，快速动作、短暂字幕和镜头切换仍需人工核对。
- 字幕校准不是数据库事务：最终索引写入失败时，可能出现任务报告失败但字幕已替换的情况。
- 校准中刷新页面或另开标签页，当前界面不会自动恢复校准任务跟踪；任务结束后可重新打开作品。
- 本地数据目录、私人案例和真实配置不能作为公开示例上传。托管登录、访客额度及隔离要求见 [登录、试用与数据隔离](DEPLOYMENT-PRIVACY.zh-CN.md)，服务器教程见 [Linux 部署](deploy/README.zh-CN.md)。

## 官方接口依据

- [Speech SDK 词级时间与最终识别结果](https://learn.microsoft.com/en-us/azure/ai-services/speech-service/get-speech-recognition-results?pivots=programming-language-python)
- [Speech 对话转写与说话人区分](https://learn.microsoft.com/en-us/azure/ai-services/speech-service/get-started-stt-diarization)
- [Speech 自定义端点](https://learn.microsoft.com/en-us/azure/ai-services/speech-service/speech-services-private-link#construct-endpoint-url)
