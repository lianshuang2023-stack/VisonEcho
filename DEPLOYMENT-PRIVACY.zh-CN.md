# 登录、试用与数据隔离

VisionEcho 提供两种运行方式：本地工作区用于维护者自己的素材；托管模式使用登录账号或访客会话，每个身份拥有独立的文件目录和索引。托管模式不会读取或导入本机 `.local-data/`。

## 用户流程

- **先试用**：无需注册，创建独立访客工作区。会话有效 24 小时，支持最多 5 次上传，每个视频不超过 1 GB（1024 MB）/ 60 秒，最多 5 次 AI 处理（生成、重新配音、校准或人物识别共用额度）。已有访客按新上限减去已使用次数计算剩余额度，不重置使用记录。退出或清除 Cookie 后不能找回未注册的访客工作区。
- **注册保存**：用户名与密码注册，可将当前有效访客工作区转为账号工作区，保留作品。
- **登录**：只显示该账号作品。登录其他账号不会合并此前访客素材。
- **退出**：服务端立即撤销当前会话，原媒体链接也需要重新登录才能访问。

账号密码使用随机盐和 scrypt，Cookie 为 HttpOnly、SameSite=Lax，在 HTTPS 下启用 Secure。会话令牌只存储哈希。账号会话有效 7 天。当前未提供邮箱验证、找回密码、多因素登录或管理员查看全部作品的入口。

## 托管启动

从干净的代码检出构建前端、安装后端依赖。不要复制本机案例目录或 `.env.local`，服务密钥通过部署平台的秘密配置注入。示例变量见 [.env.hosted.example](.env.hosted.example)。

```bash
npm --prefix dashboard/frontend ci
npm --prefix dashboard/frontend run build
.venv/bin/python -m pip install -r local_backend/requirements.txt
PUBLIC_ORIGIN=https://your-domain.example HOSTED_DATA_DIR=/srv/visionecho-data ./run-hosted.sh
```

默认后端监听 `127.0.0.1:8000`，由 HTTPS 反向代理转发。生产构建页面与 API 使用同一个域名；代理应保留 Host，严格配置可信代理，勿启用共享缓存。`PUBLIC_ORIGIN` 必须是精确的 HTTPS 来源；本机验证可使用 HTTP loopback。`HOSTED_DATA_DIR` 与 `LOCAL_DATA_DIR` 不能相同或互相嵌套。

托管目录结构：

```text
hosted-data/
  access/access.sqlite3       # 账号、会话哈希及限流记录
  workspaces/<随机工作区 ID>/ # 各自的视频、任务、字幕、人物卡及关键帧
```

当前 JSON 工作区存储只支持单进程，`run-hosted.sh` 强制一个 worker，并使用目录锁阻止第二个实例。所有用户共享一个媒体处理槽，避免同时运行多个重型任务。该版本适合小规模验证，上线规模扩大前需改为持久化任务队列及数据库。

## 私人案例如何保护

作品、上传令牌、原片、成片、字幕导出、关键帧、人物卡、回收站与后台任务全部经会话选择工作区；请求中传入其他用户 ID 无效。所有私人 API 和媒体响应禁用共享缓存。数据不放在公开静态目录。

`.gitignore` 排除本地 / 托管数据及真实环境配置；`.dockerignore` 使用源码允许列表阻止案例进入构建上下文。其他部署工具仍需从干净检出上传。现有本机案例原样保留，不会自动变成线上示例。

## 运行边界

访客创建和注册、登录尝试有服务端限流；访客 AI 配额按工作区持久化。公开上线仍应在入口配置访问速率与总 Azure 预算限制，注册账号当前不设 AI 总额度。访客会话过期后无法访问，但素材不自动删除；需为实际服务另行配置保留周期与清理任务。

已使用隔离测试验证两个账号、两个访客、退出、过期、跨账号原链接、Range 视频请求、字幕 / 关键帧 / 人物卡 / 校准任务访问，以及注册保留访客作品。测试不替代部署平台的 HTTPS、反向代理及存储配置验证。
