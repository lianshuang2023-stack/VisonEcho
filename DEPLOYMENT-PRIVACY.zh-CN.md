# 登录、试用与数据隔离

[English](DEPLOYMENT-PRIVACY.md) | **简体中文**

VisionEcho 提供本地和托管两种运行方式。本地工作区用于操作者自己的素材；托管模式为登录账号和访客会话分配独立文件目录及索引，不读取或导入维护者电脑的 `.local-data/`。

## 用户流程与试用额度

网站根地址先打开英文介绍页，可切换中文，再选择进入工作台、登录、注册或访客体验。界面语言不会改变字幕或解说语言。

- **先试用**：无需注册，创建独立访客工作区。会话有效 24 小时。
- **注册保存**：使用用户名与密码注册，可把当前有效访客工作区转为账号工作区，保留作品。
- **登录**：只显示该账号作品。登录已有账号不会合并此前访客素材。
- **退出**：服务端撤销当前会话；原媒体链接也需要有效会话才能访问。访客退出、会话过期或清除 Cookie 后，无法找回未注册工作区。

| 访客限制 | 当前额度 |
|---|---|
| AI 操作 | 共 5 次 |
| 上传 | 最多 5 次 |
| 单文件大小 | 1 GiB，即 1024 MiB / 1,073,741,824 字节 |
| 单视频时长 | 最长 60 秒；部署上限更低时取更低值 |

生成、重新配音、字幕校准、人物识别和单段 AI 改写共用 AI 操作额度，不是每种功能各 5 次。额度记录保存在工作区；已有访客按当前上限减去已用次数计算剩余量，不会因刷新或额度调整而重置。被接受的处理任务可能消耗次数，即使后续生成失败。

审校和质量提示为可选辅助，不是下载门禁。已有生成文件可以直接导出，未保存草稿不会自动写入下载的成片。本地模式无访客次数限制；注册账号当前也没有 AI 总次数上限，仍受部署的文件大小、时长与单任务处理槽约束。

## 账号与会话

密码长度为 12–24 个字符，使用随机盐和 scrypt 保存。Cookie 使用 HttpOnly、SameSite=Lax，在 HTTPS 下启用 Secure。会话令牌仅保存哈希，账号会话有效 7 天。

当前未提供邮箱验证、找回密码、多因素登录或管理员查看全部作品的入口。账号、访客与素材隔离不等于素材自动清理或备份。

## 托管启动

从干净代码检出构建前端和安装后端依赖。不要复制本地案例、`.local-data/` 或真实 `.env.local`；服务密钥通过部署平台秘密配置注入。变量模板见 [.env.hosted.example](.env.hosted.example)，完整容器教程见 [Linux 部署](deploy/README.zh-CN.md)。

安装 Python 3.12、Node.js 22 或 24 LTS、FFmpeg / FFprobe，并从仓库根目录执行：

```bash
python3.12 -m venv .venv
.venv/bin/python -m pip install -r local_backend/requirements.txt
npm --prefix dashboard/frontend ci
npm --prefix dashboard/frontend run build
PUBLIC_ORIGIN=https://your-domain.example HOSTED_DATA_DIR=/srv/visionecho-data ./run-hosted.sh
```

将示例域名和目录替换为实际配置，并通过运行环境提供 Azure 配置。后端默认监听 `127.0.0.1:8000`，由 HTTPS 反向代理转发。生产页面与 API 使用同一域名。代理必须保留 Host、仅信任实际代理来源，并关闭共享缓存。`PUBLIC_ORIGIN` 必须是精确的 HTTPS 来源；本机验证可使用 HTTP loopback。`HOSTED_DATA_DIR` 与 `LOCAL_DATA_DIR` 不能相同或相互嵌套。

托管目录结构：

```text
hosted-data/
  access/access.sqlite3        # 账号、会话哈希和限流记录
  workspaces/<随机工作区 ID>/  # 独立的视频、任务、字幕、人物卡和关键帧
```

当前 JSON 工作区存储只支持单进程。`run-hosted.sh` 强制一个 worker，目录锁阻止第二个实例使用同一托管目录。所有用户共享一个媒体处理槽。扩大服务规模前，需要持久化任务队列与数据库。

## 数据传输与私人案例

账号工作区隔离不会把云端 AI 处理变成本地计算。生成会把所需音频、关键帧和文字发送到已配置的 Azure 服务；重新配音、字幕校准、人物识别或 AI 改写发送各自所需内容。使用无对白模式会跳过语音识别，但仍会调用画面模型和解说语音合成。

作品、上传令牌、原片、成片、字幕、关键帧、人物卡、回收站和后台任务均通过会话选择工作区。传入其他用户 ID 不能切换身份。私人 API 和媒体响应禁用共享缓存，数据不放在公开静态目录。

`.gitignore` 排除本地 / 托管数据和真实环境配置；`.dockerignore` 使用源码允许列表，阻止私人素材进入容器构建上下文。经筛选的公开介绍页演示素材会明确纳入构建，并附来源及许可说明。其他部署工具也必须从干净代码检出发布。不要将私人素材复制到公开目录、仓库、镜像、介绍页截图或演示资源；公开演示只使用合成或明确授权的素材。维护者现有本地案例会保留，但不会自动成为线上示例。

## 运行边界

访客创建、注册和登录尝试有服务端限流，访客 AI 额度按工作区持久化。公开服务还需在入口配置速率限制和 Azure 总预算，尤其是注册账号目前没有 AI 总额度限制。访客会话过期后无法访问，但素材不会自动删除；实际服务需另行配置保留周期、清理和备份。

隔离测试覆盖多个账号与访客、退出和过期、跨账号原链接、Range 视频请求、字幕 / 关键帧 / 人物卡 / 校准任务，以及注册保留访客作品。测试不替代实际部署的 HTTPS、反向代理和存储配置验证。
